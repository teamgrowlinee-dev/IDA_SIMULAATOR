.DS_Store
node_modules/
npm-debug.log*
yarn-error.log*

# Local environment secrets
.env
.env.*
!.env.example

# Build outputs
app/dist/
netlify-dist/
# widget dist is committed so Render doesn't need vite in build step
# packages/widget/dist/
